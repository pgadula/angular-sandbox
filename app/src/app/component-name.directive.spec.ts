import { ComponentNameDirective } from './component-name.directive';

describe('ComponentNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentNameDirective();
    expect(directive).toBeTruthy();
  });
});
